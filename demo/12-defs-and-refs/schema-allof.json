{
  "$defs": {
    "HasChecksum": { "required": ["checksum"] },
    "HasPrevious": { "required": ["previous"] }
  },
  "allOf": [
    { "$ref": "#/$defs/HasChecksum" },
    { "$ref": "#/$defs/HasPrevious" }
  ]
}
