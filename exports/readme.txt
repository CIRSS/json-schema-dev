json-schema-dev — a REPRO capability module bundling two JSON Schema
validators (python-jsonschema and Ajv, both 2020-12) behind one command-line
contract, so the same schema and instance can be put to both and their
verdicts compared.
