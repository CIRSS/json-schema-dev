# json-schema-dev

A [REPRO](https://github.com/repros-dev) capability module that bundles two JSON Schema validators and a gallery of minimal, runnable demonstrations of individual JSON Schema constructs. Each demo is one `(schema, instance)` pair validated through **both** validators; the committed `run.txt` golden file records the two verdicts side by side. Divergences in verdicts recorded in the golden files are expected and called out explicitly in the demo documentation cells. Re-run a demo and `git diff` its golden file to confirm that neither validator's behavior has changed since the golden was committed.

## Validators

Both pinned, both driven at the **JSON Schema 2020-12** dialect:

| Language | Library | Command |
| --- | --- | --- |
| Python | [`jsonschema`](https://pypi.org/project/jsonschema/) `4.23.0` (`Draft202012Validator`) | `jsonschema-validate --schema FILE --instance FILE [--ref FILE]...` |
| JavaScript | [`ajv`](https://ajv.js.org/) `8.17.1` (`ajv/dist/2020`) | `ajv-validate --schema FILE --instance FILE [--ref FILE]...` |

Arguments are **named** (`--schema`/`--instance`, or `-s`/`-i`), not positional, so the schema and instance can't be transposed by accident — a swap that would otherwise pass silently, since any JSON object is itself a valid, permissive schema.

Each `--ref FILE` (repeatable) loads an additional schema and registers it under its `$id`, making it available to `$ref` in the main schema — this is how a schema spread across several files is validated. A `--ref` schema must carry a `$id`; nothing is ever fetched from the `$id`'s URI, which serves purely as a name to resolve against (demo 16).

Neither wrapper checks `format`: it is the annotation the 2020-12 specification makes it by default, on both legs alike and in silence (Ajv's "unknown format ignored" warning is switched off, since ignoring is all the wrapper ever does with a format). Format assertion, if ever added, will be added to both wrappers together as an explicit option, with the unknown-format case handled identically on both.

Both wrappers honor the nonstandard `errorMessage` keyword (a portable subset of [`ajv-errors`](https://github.com/ajv-validator/ajv-errors): a plain string covering its subschema, or an object keyed by keyword, either containing `${pointer}` interpolations that JSON-encode an instance value — absolute JSON Pointers resolving from the instance root, and relative ones resolving from the failing value's own location, so `${0}` is the failing value itself): Ajv through the plugin, the Python wrapper by reading the messages directly out of the schema JSON. Failures no message covers keep each library's own message (demo 17).

Each command writes its verdict to stdout — `VALID`, or one `INVALID: <error>` line per validation error — and exits **0** (valid), **1** (invalid), or **2** (error: bad arguments, an unreadable file, a schema or `--ref` file that is not valid JSON, an invalid schema, or an unresolvable `$ref`); diagnostics and `-h`/`--help` text go to stderr/stdout respectively. An *instance* file that reads but does not parse as JSON is a **verdict**, not an error — `INVALID: instance is not valid JSON: …`, exit 1 — because everything about the submitted document is a verdict and everything about the setup is an error; a document failing the JSON grammar itself has failed validation's lowest tier (demo 18). In the same spirit, `--reject-duplicate-members` makes a duplicate object member name anywhere in the instance a verdict (`INVALID: instance contains duplicate member name "…"`, all names sorted, word-for-word identical across the two wrappers): parsers silently keep the last value, so no schema can ever see a duplicate — parse time is the only tier that can check (demo 19). On the schema side, a schema (or `--ref` file) whose `$schema` entry declares any version other than 2020-12 is refused (exit 2) rather than silently reinterpreted; `--ignore-declared-version` discards the declaration and validates as 2020-12 anyway, making the reinterpretation an explicit choice (demo 20). So the two are directly comparable *and* safe to script. The dialect is forced in the wrapper rather than read from the schema's `$schema`, so an old validator cannot silently fall back to a weaker dialect.

Both commands are exported as artifacts of this module, so they are on `PATH` inside this REPRO **and** inside any REPRO that composes it with `repro.require json-schema-dev …` — the module's capability is the uniform cross-validator CLI, not just the two libraries.

## Demos

**What JSON is.** A JSON text is a serialization of exactly one value, and there are six kinds of values: object (JavaScript's legacy name for a plain string-keyed map), array, string, number, boolean, and `null`. Any of the six can be the entire document — the minimal JSON document is not `{}` but a bare primitive like `5` or `null`. JSON's syntax descends from JavaScript and is (since 2019) literally a syntactic subset of JS expressions, but the converse fails: `undefined`, `NaN`, `Infinity`, unquoted keys, single-quoted strings, trailing commas, and comments are all assignable JavaScript and all rejected by every JSON parser. Numbers are arbitrary-precision decimal in the grammar — implementations impose the precision limits, and `NaN`/`Infinity` are inexpressible. Demo 01 shows bare primitives accepted as complete documents; demo 18 shows what becomes of a document that fails this grammar.

**What JSON Schema is.** A schema is a predicate over JSON values: handed one instance, it accepts or rejects, and that is all it does. Keywords only ever *remove* instances from the accepted set, so the schema that says nothing — `{}` — accepts everything, and each added keyword narrows: a schema object is an *and* of its keywords, every member enforced at once — even when, as several demos show, a particular member happens to remove nothing. Openness is the default, and closing a schema is its own deliberate act (demo 14). Keywords are type-scoped and pass silently on instances of other kinds (demo 05). Every position that takes a schema takes *any* schema — each `properties` entry, array item schema, `$defs` definition, and `allOf` branch is a complete schema in its own right (demo 09) — so large schemas are compositions of small predicates, and reuse and extension are mechanical rather than clever. Annotations describe but can never reject; only assertions reject (demo 06). Everything else in the language is particular keywords playing one of these two roles inside that recursive shape.

The grammar below captures this whole recursive shape, and it is how to read a schema. Because everything here is JSON, all the way down, no key tells you its role by its spelling: `"type"` is a keyword as a member of a schema, and an ordinary name one level down, as a member of the value of `properties`. To tell the roles apart, read the document from its root with the grammar in hand: start at `SCHEMA`, and each key's role follows from the rule that admitted it:

```
SCHEMA  ::= true | false | { MEMBER, ... }
MEMBER  ::= KEYWORD : VALUE            a keyword the standard defines
          | NON-KEYWORD : JSON         any other key; ignored by validation
VALUE   ::= JSON                       assertions, annotations, identifiers
                                       (type, enum, const, pattern, required,
                                        title, description, $id, $ref, $schema, ...)
          | SCHEMA                     single-schema applicators
                                       (items, additionalProperties, not,
                                        if, then, else, unevaluatedProperties, ...)
          | [ SCHEMA, ... ]            list applicators (allOf, anyOf, oneOf, prefixItems)
          | { NAME : SCHEMA, ... }     map applicators; NAME is any string
                                       (properties, patternProperties, $defs, ...)
```

An object `SCHEMA` is the *and* of its members. Each keyword either asserts (may reject), annotates (never rejects), identifies or refers, or applies subschemas and combines their verdicts. `true` and `false` at the top are the same schemas that appear inside any `SCHEMA` slot — demo 01's boolean schemas and demo 09's non-boolean `additionalProperties` are one fact seen twice — and a `NON-KEYWORD` member is carried but never consulted, which is where the nonstandard `errorMessage` of demo 17 lives.

The demos are ordered as a curriculum: **no construct appears in a demo before it has been the focus of an earlier one**, so reading the `run.txt` goldens in numeric order is a complete, self-contained course — from what a schema *is* through the composition machinery a real vocabulary schema is built from. One flagged exception: demo 07 borrows `not` one demo ahead of demo 08, says so in place, and gives it the one-line meaning the moment needs.

| # | Construct | Shows |
| --- | --- | --- |
| `01-empty-and-boolean-schemas` | `{}`, `true`, `false` | schemas as JSON, admitted by the meta-schema; accept-all and reject-all; any JSON value kind as a complete instance; unknown members ignored; one malformed keyword value and it is no schema |
| `02-types-and-properties` | `type`, `properties` | `type` on the whole instance, single or listed; a bare string as a valid document; `properties` as name-to-schema map that neither requires nor forbids |
| `03-arrays-items-and-minitems` | `items`, `minItems` | `items` as the array counterpart of `properties`; schema-inside-schema nesting; `[]` satisfying any `items`; `minItems` as floor, not count |
| `04-empty-vs-nonempty-object` | `required` | `{}` accepted by `properties` alone; `required` as presence, not content; `{"id": null}` rejected only by a value constraint |
| `05-null-and-applicability` | `minLength`, `minimum`, `enum`, `null` in a `type` list | type-scoped keywords passing silently on other kinds; `null` admitted only by naming it in `type`; `null` exempt from string constraints; `enum` as the exception |
| `06-annotations-vs-assertions` | `title`/`description`/`$comment`, `format` vs `pattern` | assertions vs annotations; the three prose keywords; a contradicting description unremarked; `format` as annotation, unchecked by both wrappers; `pattern` as the enforced form |
| `07-pattern-pitfalls` | `pattern` | regex notation glossed; four pattern pitfalls: unanchored patterns; Python's `$` before a trailing newline (remedies: `$(?!\n)`; `"not": {"pattern": "\n$"}` beside the pattern, universal); POSIX classes as schema error vs silently different predicate; `\d` and Arabic-Indic digits; the regex subset both engines share |
| `08-anyof-and-not` | `anyOf`, `not` | a schema object as an *and* of its keywords; the five schema-combining keywords named; `anyOf` over two patterns vs one `|` alternation (`( )`, `|`, `[.]` glossed); `not` beside a pattern as negation without lookahead |
| `09-schemas-in-schema-position` | `additionalProperties` | the schema every extra property must satisfy (`{"type": "number"}` shown first); `false`, `true`, and `{}` as the accept-all/reject-all schemas in the same position |
| `10-annotated-constants` | `oneOf`, `const` vs `enum` | alternation over documented constants; accepting and rejecting exactly what a flat `enum` does; per-value prose; noisier errors; an `enum` of mixed kinds, matched by JSON equality (`2` vs `"2"`) |
| `11-tagged-unions` | `if`/`then`/`else` | a tag property selecting constraints; `if` must `require` its tag or `{}` falls into `then`; `if` on the instance's own kind (`type: string` → `maxLength`, else `maximum`); member order immaterial; `then` and `else` silently inert without `if`; the trio desugared to `anyOf`/`allOf`/`not` |
| `12-defs-and-refs` | `$defs`, `$ref`, `allOf` | a definition written once, enforced everywhere; the `#/$defs/...` path glossed; composition by `allOf`; a typo'd `$ref` failing at compile time in Ajv, on first use in `jsonschema` — and not at all on paths no instance takes (VALID from one leg, schema error from the other) |
| `13-identifiers-and-refs` | `$id`, `$anchor`, `$ref` | `$id` as identity; a URI-form `$ref` matched against `$id`s the validator already holds, nothing fetched; `$anchor` names; one word reached three ways (`#/$defs/named`, `#named`, bare `named`) — the bare form resolving away from the definition beside it; the four rules |
| `14-unevaluated-properties` | `unevaluatedProperties: false` vs `additionalProperties: false` over `allOf` | `additionalProperties: false` beside `allOf` reading only its own schema object, rejecting the objects the branches describe; `unevaluatedProperties: false` counting every property a branch checks; a closed schema still requiring nothing (`{}` passes); `required` added beside `unevaluatedProperties`, making `a` and `b` mandatory while `c` stays blocked; pre-2019-09 validators ignoring the keyword as an unknown member |
| `15-closure-scope` | `$ref`, `$defs`, `unevaluatedProperties` vs `additionalProperties` | closing affects only the object being validated: members described in a `$defs` definition support closing, but only with `unevaluatedProperties`; `required` beside `additionalProperties` yields a schema no object satisfies (☠); a nested object is closed only by its own subschema; close only where extra members must be rejected, with a `$comment` beside each closing keyword — capped by a commented schema using each keyword where it is the simplest that works |
| `16-multi-file-composition` | `--ref`, `$ref` by `$id`, `unevaluatedProperties` across files | a component schema in its own file, open and usable on its own — accepting members it does not describe, still rejecting a record without `id`; a composite applying the component with `$ref` in an `allOf` branch, requiring `status`, and closing the whole schema — both schemas' requirements in force, members the component evaluates not extra; each validator handed the component file via `--ref`; with one component the `allOf` is optional (a `$ref` beside other keywords applies alongside them, 2019-09 on) but kept — pre-2019-09 validators silently ignore members beside a `$ref`, and only `allOf` accommodates several components; omitting `--ref` an error, not a verdict — the `$ref` resolves to nothing; a two-level capstone: the component applied to record and nested creator by the same `$ref`, the creator's `id` tightened by `pattern` to URI form beside the component's string requirement (`format` being annotation-only), each object closed by its own subschema, rejections at `/creator/id` and `/creator`; a closing ☠: adding `note` through the closed composite yields a schema no record satisfies — evaluations outside a composite are invisible to its own closure — so components stay open and only final products close |
| `17-custom-error-messages` | `errorMessage` (nonstandard) | message text left to validators by the standard; authored messages in the schema, honored by both wrappers; `${0}` quoting the failing value inside arrays |
| `18-non-json-instances` | the wrappers' exit-code contract | non-JSON instance as verdict (exit 1), missing file as error (exit 2); instance facts are verdicts, everything else errors; parser detail diverging like all message prose |
| `19-duplicate-member-names` | `--reject-duplicate-members` | last-value-wins parsing, invisible to any schema; a `const` on the last value passing; the flag as parse-time verdict, identical across wrappers |
| `20-declared-versions` | `$schema`, `--ignore-declared-version` | non-2020-12 declarations refused, not reinterpreted; draft-07 `definitions` as ignored unknown members; the override as explicit choice |

Each demo is built as a controlled experiment: exactly one thing varies between the cases it compares, and everything else — including the instances used as backgrounds — is held constant and deliberately unremarkable. A demo that changed two things at once would report verdicts that could not be attributed to either.

Where the two validators genuinely diverge — from library behavior, host-language differences, or latitude in the standard — the divergence is taught in the demo that owns the construct rather than collected in one place. Current examples: demo 07 (three verdict-level divergences: Python's `$` accepts a trailing newline; a POSIX character class is rejected as an invalid schema by Ajv while Python silently computes a different predicate from it; and `\d` is Unicode-aware in Python and ASCII-only in JavaScript, so Arabic-Indic digits satisfy `^\d+$` under one engine and not the other), and demos 12 and 13 (an unresolvable `$ref` is a compile-time error in Ajv but surfaces lazily, during validation, in `jsonschema`). Demo 17 is the family's inverse: message text is divergent by default — the standard leaves it to each implementation — and the demo shows a schema-authored `errorMessage` closing that divergence deliberately, with both wrappers emitting the same sentence.

Each demo's `run.sh` is a small shell notebook built from two cell helpers defined in [`demo/cells.sh`](demo/cells.sh): `doc`, which prints a prose cell from a heredoc, and `show`, which prints a command and its output. The prose is therefore part of the captured `run.txt`, so a golden file reads as a self-contained lesson — what the construct does and when to reach for it, followed by the evidence — rather than a bare transcript.

Two further helpers are not cells. `title`, the first line of every `run.sh`, prints the notebook's title once at the top so a golden says what it is (a selective run is an excerpt and carries no title). `section` prints a distinct `#####` header dividing a demo into groups of cells that go together, numbered on its own and not counted in cell numbering; a header is printed lazily, just before the first selected cell after it, so a selective run shows only the sections it touches. The three banner styles form a visual hierarchy — `#####` for sections, `=====` for prose (`doc`) cells, `-----` for command (`show`) cells — so grouping, teaching, and evidence can be told apart while scrolling. Cells are numbered in their banners (`===== [5] …`, `----- [6] …`), so a golden doubles as the demo's cell index, and `run.sh` accepts a cell selection for interactive experiments: each argument is a cell number or an inclusive range, freely mixed — `bash run.sh 5`, `bash run.sh 1 2 3`, `bash run.sh 2 5-8 12` — while `bash run.sh` alone runs every cell (the form the goldens record). Cells always run in document order whatever the argument order. Selection assumes cells are independent — a demo must not let one cell depend on another's side effects.

## Build and run

The parent image adds Python (via `apt`) and a pinned Node (official prebuilt binary) to the published framework base, so it builds from nothing local; the demo runner (via `shell-notebook`) is composed at build time.

```
make build-parent     # framework base + Python + Node (one-time, slow layer)
make build-image      # compose shell-notebook and this module's validators
make run-demo         # run every demo/NN-*/run.sh, capturing output
```

On the first `make run-demo`, review each demo's output and commit its `run.txt` as the golden file; subsequent runs diff against it.
